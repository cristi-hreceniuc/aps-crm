spring:
  datasource:
    url: jdbc:mysql://localhost:3306/crm
    username: wordpress
    password: recell123.A.
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  application:
    name: aps-crm
    profiles:
      include: local, default
    jpa:
      open-in-view: false
      show-sql: false
      hibernate:
        ddl-auto: none
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          format_sql: true
    jackson:
      serialization:
        WRITE_DATES_AS_TIMESTAMPS: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: someone@email.com
    password: ceva ceva ceva
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

security:
  jwt:
    secret-key: 7608086fdd5b9f3794511cdc8295da52de723ff71dd859f8d4845554550f5a98
    expiration-time: 3600000   # 1h in millisecond
    refresh-expiration-time: 2592000000  # 30 zile (refresh)

logging:
  level:
    org.springframework.web: DEBUG
    org.hibernate: ERROR
    org.springframework.security: DEBUG
    org.springframework.mail: DEBUG
    org.springframework.mail.javamail: DEBUG
app:
  reset:
    frontend-url: "http://51.91.56.46:8080/api/v1/auth/reset?token=" # pagina ta de UI unde utilizatorul introduce parola nouă
    ttl-minutes: 60
  otp:
    ttl-minutes: 15
    max-attempts: 5
    lockout-minutes: 15

# AWS S3 Configuration for private asset storage
aws:
  s3:
    bucket-name: logopedy-assets
    region: eu-north-1
    access-key: YOUR_AWS_ACCESS_KEY
    secret-key: YOUR_AWS_SECRET_KEY
    presigned-url-expiration-minutes: 60  # Pre-signed URLs valid for 60 minutes